#ifdef INTERFACE_H
#define INTERFACE_H
#include <string>

class TreeInterpreterInterface {
    public:
    TreeInterpreterInterface();

    int Save(std::string name, std::fileName);
    int Contains <tree1> <tree2>

    private:
}

#endif //INTERFACE_H